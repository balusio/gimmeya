/* eslint-disable import/no-unresolved */
import ReactDOM from 'react-dom';
import App from './app';
// eslint-disable-next-line no-undef

ReactDOM.render(App(), document.getElementById('gimmeApp'));
